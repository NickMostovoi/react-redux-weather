export const currentWeather = (state) => {
    return state.currentWeather;
};

export const searchHistory = (state) => {
    return state.searchHistory;
};

export const displayContent = (state) => {
    return state.displayContent;
};